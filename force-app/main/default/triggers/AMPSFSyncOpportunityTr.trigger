trigger AMPSFSyncOpportunityTr on Opportunity (after insert, after update) {
  /*  Set<Id> ids = new Set<Id>();
    for (Opportunity Opp : Trigger.new) 
    { 
        ids.add(opp.Id);
    } 
    if(ids.size() > 0)
    {
        //if below line is uncommented then trigger will sync this opportunity to AMP
       // AMPUpdateOpportunityFromPortalUser.UpdateOppHelper(ids);
    }*/
}